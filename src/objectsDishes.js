const fs = require("fs");
const allDishesDate = require("../allDishesDate.json")
const typeDishesDate = require("../typeDishesDate.json")

export const typeDishes = typeDishesDate

export const dishes = allDishesDate